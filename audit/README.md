# Audit logs

This folder is tracked as a placeholder for local development.

Runtime audit logs are written to `audit/audit.jsonl`, and production evidence should be stored in runtime storage (for example Drive) rather than committed to Git.
