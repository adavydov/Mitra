services:
  - type: web
    name: mitra-mvp
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn mitra_app.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
